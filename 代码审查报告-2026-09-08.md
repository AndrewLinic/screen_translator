# 代码整体性审查报告

日期：2026-09-08 ｜ 范围：全部源码（约 250KB / 17 个模块）｜ 方式：三路并行通读 + 人工核实

> **状态更新（2026-09-08 21:20）：P1 三项已全部修复**，冒烟测试 11/11 通过（test_smoke_v5.py），已重新打包替换 dist。
> - P1-1：`select_region_interactive` 返回前经 `_to_physical` 换算；`region_overlay.set_region` 按 devicePixelRatio 把物理坐标转回 Qt 逻辑坐标（报告原方案漏了这一半，只修选区会让指示框错位）。
> - P1-2：`_setup_file_logging` 解析出的目录存入模块级 `_LOG_DIR`，`_export_feedback` 优先取用，兜底反查 root logger。
> - P1-3：新增 `self._state_lock`；调度线程取状态/写结果/清 pending 全部加锁并用 gen 代数校验，状态表重置后 in-flight 结果直接作废。

## 总体评价

架构清晰、职责划分合理：main（调度）→ subtitle（展示）→ translator/ocr（管线）分层干净。今天重构后的多区域并发设计（每区域独立状态 + FIFO 队列单调度线程串行消费）方向正确；worker 子进程协议（JSON stdout + stderr 落盘 + id 匹配 + 超时杀进程）健壮性良好。**未发现 P0 级必崩问题**；配置键名全量核对无失配；13 个信号全部正确连接、无重复 connect、lambda 无循环变量陷阱。发现 3 个 P1 隐患和一批 P2/死代码。

## P1 — 应尽快修

### 1. DPI 缩放下选区坐标错位（今天重写引入的回归）
`screenshot.py:321` 返回 `rect.x() + sx` 把**逻辑坐标**直接加**物理坐标**；`_to_physical`（screenshot.py:141）写好了却从未调用。而 `main.py:1064` 开启了 `AA_EnableHighDpiScaling`。
- 100% 缩放下两者相等，无感；**125%/150% 缩放下选区会系统性偏移**（旧版代码有 kx 换算，重写时丢失）。
- 修法：返回前用 `_to_physical(rect)` 换算再加 `(sx, sy)`。

### 2. 反馈诊断包永远缺日志
`main.py:766` 从 `logging.getLogger("screen_translator").handlers[0]` 取日志路径，但 `_setup_file_logging`（main.py:1042）把 FileHandler 挂在 **root** logger 上，具名 logger 永远为空 → 每次都回退到 `data_dir()`，zip 里永远不含 `app.log`/`stderr.log`。
- 修法：改查 `logging.getLogger().handlers`。

### 3. `_region_state` 跨线程无锁读写
`main.py:918`（调度线程写 `pending`）与 `main.py:821/605/702`（GUI 线程读写同区域 dict）并发；区域列表重置时 in-flight 结果会写进已被丢弃的旧 dict。CPython 单字节码原子性挡不住"检查-再写"序列。
- 修法：每区域一把 `threading.Lock`，或约定状态字段仅 GUI 线程写、worker 结果一律经信号回传。

## P2 — 建议修

> **状态更新（2026-09-08 21:58）：P2 全部 14 项已修复**，冒烟 v6 26/26 + v5 回归 11/11 通过，已重新打包。逐项落点见 `.workbuddy/memory/2026-09-08.md`。

| # | 位置 | 问题 | 状态 |
|---|---|---|---|
| 4 | subtitle.py `_toggle_pause` | 暂停分支未 `set_busy(False)`，spinner 定时器暂停期间仍在跑（视图已隐藏，仅空耗） | ✅ 已修 |
| 5 | subtitle.py:884 | `QTimer.singleShot(15000, lambda...)` 强引用 self，关窗后 15s 内无法释放 | ✅ 已修（改父属 QTimer，随窗销毁） |
| 6 | subtitle.py:175-180 | `btn_copy_orig/btn_history/btn_vocab/btn_speak` 创建后从未加入工具栏——孤儿控件（功能已在"⋯"菜单实现），应删除 | ✅ 已删（含 btn_copy_trans 共 5 个） |
| 7 | subtitle.py:1015,384 / 115 | 死代码：`_highlight_words`、`set_font_size` 无调用方；`request_clear` 信号无任何 emit 点 | ✅ 已删（后补：同时清了 main.py:167 的 connect 与 _after_clear 死方法，否则启动崩） |
| 8 | ocr.py:434 | 断裂合并用 `prev + tok` 而非去标点后的 `a + b`，"Exped. ition"→"Exped.ition" | ✅ 已修（保留前导/尾随标点） |
| 9 | ocr.py:443 | 大小写矫正可能伤及 `macOS`（内部大写≥2 且若 ecdict 收录小写形式）；建议加 `Mac/Mc` 前缀豁免兜底 | ✅ 已修（Mac/Mc 前缀豁免） |
| 10 | screenshot.py:204 | 放大镜取样 `sx/sy` 未 clamp，屏幕边缘为负时 `copy` 越界区域未定义 | ✅ 已修（clamp 到背景图范围） |
| 11 | screenshot.py:290 | `_confirmed` 从未读取（且未在 `__init__` 初始化）；`grab_virtual_desktop` 无异常保护 | ✅ 已修（删变量 + mss 失败退 Qt 抓屏） |
| 12 | main.py:327 / region_overlay.py:72 / config.py:108 | 死代码：`old` 变量、`current_region`、`update_config` 无调用方 | ✅ 已删 |
| 13 | hotkeys.py:122-136 | `unregister_all` join 超时后清空 `_registered`，旧线程来不及反注册 → 热键泄漏；WM_QUIT 分支不可达 | ✅ 已修（线程用本地副本反注册 + join 3s + 删不可达分支） |
| 14 | region_overlay.py:51 | `set_region(color=None)` 不复位为默认色，单区域模式沿用上次多区域颜色 | ✅ 已修（color=None 复位默认红） |
| 15 | translator.py:513,494 | `_request` 全程持锁，worker 卡顿时阻塞所有调用线程；`_kill` 不关 `_stderr_fh` | ✅ 已修（发请求持锁、等响应按 id 分发不持锁；kill 关句柄） |
| 16 | subtitle.py:1208 | `restore_geometry` 只按主屏校验，副屏位置在拔掉副屏后丢失（可遍历所有 screen） | ✅ 已修（遍历所有屏，重叠≥60px 即恢复） |
| 17 | subtitle.py:581 | 多区域 chips 样式固定白字，浅色主题下非激活 chip 不可见 | ✅ 已修（浅色主题深色系样式，主题切换重刷） |

## 核实说明

三路审查共报 32 条，逐条核实后：采纳上表；剔除 2 条不成立论断（McDonald 误改、DPI"待验证"升级为确认回归）；另确认 4 项**没有问题**：点击穿透防循环、spinner token 失效、worker 协议健壮性、全部 `cfg.get` 键名与 DEFAULT_CONFIG 一致。

## 建议修复顺序

P1 三项一次修完（改动都很小、互相独立）→ P2 的 6/8/10/11（死代码清理 + 边界 clamp）→ 其余择机。
